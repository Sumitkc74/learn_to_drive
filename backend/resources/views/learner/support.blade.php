@extends('learner.layout')
@section('title',__('Help & feedback'))
@section('content')
<div class="page-heading"><p class="eyebrow">{{ __('WE’RE HERE TO HELP') }}</p><h1>{{ __('Help & feedback') }}</h1><p class="lead">{{ __('Ask a question, suggest an improvement, or tell us what’s wrong.') }}</p></div><div class="two-column"><section class="card"><h2>{{ __('Send a message') }}</h2><form method="post" class="stack">@csrf<label>{{ __('Message type') }}<select name="type">@foreach(['Other','Complaint','Feature request'] as $type)<option value="{{ $type }}" @selected(old('type')===$type)>{{ __($type) }}</option>@endforeach</select></label><label>{{ __('Subject') }}<input name="subject" required maxlength="200" value="{{ old('subject') }}"></label><label>{{ __('Message') }}<textarea name="message" required maxlength="5000" rows="6">{{ old('message') }}</textarea></label><button class="button">{{ __('Send to admin team →') }}</button></form></section><section class="card"><h2>{{ __('Your conversations') }}</h2>@forelse($tickets as $ticket)<a class="ticket" href="{{ route('learn.support.show',$ticket) }}"><span class="badge">{{ __($ticket->status) }}</span><h3>{{ $ticket->subject }}</h3><span class="muted">{{ $ticket->created_at->locale(app()->getLocale())->translatedFormat('d M Y') }}</span></a>@empty<p>{{ __('No messages yet. Your conversations and admin replies will appear here.') }}</p>@endforelse{{ $tickets->links('pagination::simple-default') }}</section></div>
@endsection
