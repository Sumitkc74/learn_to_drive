<div class="page-heading"><p class="eyebrow">{{ __('CHOOSE YOUR NEXT STEP') }}</p><h1>{{ __('Start free. Grow with Premium.') }}</h1><p class="lead">{{ __('Explore as a guest, save your learning with a free account, or get extra support with Premium.') }}</p></div>
<section class="card guest-baseline"><h2>{{ __('Already available to guests') }}</h2><p>{{ __('Browse traffic signs, flashcards, vision exercises, tutorials, question-bank PDFs, exam information and current notices. No account required.') }}</p></section>
<div class="two-column plan-comparison">
<section class="card"><span class="badge">{{ __('REGISTERED (FREE)') }}</span><h2>{{ __('Your learning, saved.') }}</h2><p><strong>{{ __('NPR 0') }}</strong> {{ __('· Create an account') }}</p><h3>{{ __('Everything guests can browse, plus:') }}</h3><ul><li>{{ __('Scored practice sessions with answer review.') }}</li><li>{{ __('Saved practice history, accuracy and daily streaks.') }}</li><li>{{ __('Access your saved progress on other devices by signing in.') }}</li><li>{{ __('Personal details, email verification and optional MFA.') }}</li><li>{{ __('Content reports and private support conversations.') }}</li></ul><a class="button" href="{{ route('learn.register') }}" data-auth-open="signup">{{ __('Create a free account') }}</a></section>
<section class="card paid-plan"><span class="badge">{{ __('PREMIUM (PAID)') }}</span><h2>{{ __('Turn mistakes into understanding.') }}</h2>@if(config('payments.amount_paisa')>0 && config('payments.days')>0)<p><strong>{{ __('NPR') }} {{ number_format(config('payments.amount_paisa')/100,2) }}</strong> {{ __('for') }} {{ config('payments.days') }} {{ __('days') }}</p>@else<p>{{ __('Pricing will be available when checkout is configured.') }}</p>@endif<h3>{{ __('Everything in Registered Free, plus:') }}</h3><ul><li>{{ __('A study chatbot in English or Nepali.') }}</li><li>{{ __('Revision modules grouped by topics you answered incorrectly.') }}</li><li>{{ __('Targeted retries that clear corrected mistakes.') }}</li><li>{{ __('Existing solutions and explanations collected for focused review.') }}</li></ul><p class="muted">{{ __('Chat allows') }} {{ config('premium.daily_chat_limit') }} {{ __('requests per Nepal day. Revision uses your latest 100 completed sessions.') }}</p><a class="button" href="#premium-checkout">{{ __('View upgrade options') }}</a></section>
</div>
